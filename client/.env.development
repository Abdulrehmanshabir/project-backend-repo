# Local development settings
VITE_API_URL=http://localhost:3000

# You may keep these only in base .env if identical across envs
VITE_AUTH_LOGIN_PATH=/auth/login
VITE_AUTH_REGISTER_PATH=/auth/register

