# Production build settings
# Set this to your deployed API origin (no trailing slash)
VITE_API_URL=https://your-backend-domain.example.com

# If these differ in prod, override them here; otherwise, inherited from base .env
# VITE_AUTH_LOGIN_PATH=/auth/login
# VITE_AUTH_REGISTER_PATH=/auth/register

